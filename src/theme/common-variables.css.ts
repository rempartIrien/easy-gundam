import { fonts } from "./fonts";

const commonVars = {
	font: fonts,
	space: {
		fixed: "8px",
		relative: "0.5rem",
	},
	borderRadius: {
		base: "4px",
		round: "50%",
	},
};

export default commonVars;
